# ────────────────────────────────────────────────────────────────
#  CrowdGPT.ai — NVIDIA CUDA build
# ────────────────────────────────────────────────────────────────

torch>=2.0.0
numpy>=1.24.0
requests>=2.28.0
rich>=13.0.0
psutil>=5.9.0

# ── Fresh install only (skip if you already have torch+CUDA) ───
# The default `pip install torch` already bundles CUDA on
# Linux/Windows. For a specific CUDA build instead:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
